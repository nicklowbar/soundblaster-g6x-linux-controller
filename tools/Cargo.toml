[package]
name = "usb_send"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
rusb = "0.9"
hidapi = "2.5"
hex = "0.4"

[profile.release]
strip = true
lto = true
